osu_replay_key							t																	#retry button 
osu_file_name							hard.osu															#same  directory	def=hard.osu
debug_mode								1																	#1 				or 		0


hack_method								key_enum 														#key_enum	or		replay
key_enum_dev_event_id					/dev/input/event4												#must need if key enum
key_enum_start_way						keyboard															#mouse	keyboard	monitor
key_enum_start_action					press																#not in monitor, press or release																	
key_enum_keyboard_start				z																	#a char, work only keyboard method def=z

x_break_distance							75																	#use x if in distance

map_mode								HRHDPFNF														#HR should be there while DT not, for while
n_lock_timer								5																	#Every 4 obj, check if num lock for restart